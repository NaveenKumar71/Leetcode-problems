class Solution {
    public int findNumbers(int[] nums) {
        int ans=0;
        for(int n:nums){
            if((n>=10 && n<100) || (n>=1000 && n<10000) || (n>=100000 && n<1000000)) ans++;
        }
        return ans;
    }
}
